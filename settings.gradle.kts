dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven(url = uri("https://www.jitpack.io"))
    }
}
rootProject.name = "Neugelb App"
include(":app")
